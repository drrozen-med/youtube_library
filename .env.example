# YouTube Orchestrator - Environment Variables
# Copy this file to .env and fill in your actual API keys

# Required: YouTube Data API v3 key
# Get one at: https://console.cloud.google.com/apis/credentials
YT_API_KEY=your_youtube_api_key_here

# Optional: OpenAI API key for LLM summarization
# Leave empty if you prefer using Ollama locally
OPENAI_API_KEY=your_openai_key_here_optional

# Optional: Ollama model name for local LLM summarization
# Default: mistral
# Requires: Ollama installed and running (brew install ollama)
OLLAMA_MODEL=mistral

# Optional: Default output directory for vault
# Default: ./vault
DEFAULT_OUTPUT=./vault

# Transcript fetch mode:
# - proxy_then_direct (default): try proxies first, then youtube-transcript-api
# - proxy_only: only proxy services; fail if they fail
# - direct_only: skip proxies
TRANSCRIPT_FETCH_MODE=proxy_then_direct

# Optional: Proxy service API keys (recommended for production reliability)
SCRAPINGBEE_API_KEY=your_scrapingbee_key_here
SCRAPENINJA_API_KEY=your_scrapeninja_key_here
FIRECRAWL_API_KEY=your_firecrawl_key_here
SCRAPEOPS_API_KEY=your_scrapeops_key_here
